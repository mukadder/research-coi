var node = new Server("boot the server").console.log('server running at port");
var [] = ["8080",'8081'];
var global = new Array("object",console);


var console = "log this error";
var login= function() {

var legs=[];
for(int i=0 i<legs.length; i++){
legs.push(legs.splice(1,0);
}
